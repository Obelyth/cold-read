# Baselines (RED phase)

Captured output from subagents given each fixture WITHOUT the cold-read skill.

These are evidence, not tests. They record what Claude actually does by default,
so the skill can be written against real failures rather than imagined ones. Do not
edit them after capture. If behavior needs re-baselining, add a dated file.

Captured 2026-07-28.
